# ─────────────────────────────────────────────────
# horix-platform — Nginx configuration
# ─────────────────────────────────────────────────
# 
# Usar según el modo:
#   test → sudo cp nginx/platform-test.conf /etc/nginx/sites-available/platform
#   prod → sudo cp nginx/platform-prod.conf /etc/nginx/sites-available/platform
# 
# O ejecutar: sudo bash install.sh test|prod
